@using Microsoft.AspNet.Identity
@using BetterTaxi.Models
@if (Request.IsAuthenticated && User.IsInRole(UserRoles.Administrator.ToString()))
{
    <ul class="nav navbar-nav">
        <li>@Html.ActionLink("Dashboard", "Index", "Dashboard", new { area = "Administration" }, htmlAttributes: new { id = "adminLink" })</li>
    </ul>
}
